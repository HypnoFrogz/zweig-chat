{
  "name": "zweig-desktop",
  "version": "1.0.0",
  "description": "Zweig Desktop — the web client wrapped as a native Windows app",
  "main": "main.js",
  "author": "Zweig",
  "license": "UNLICENSED",
  "scripts": {
    "start": "electron .",
    "dist": "electron-packager . Zweig --platform=win32 --arch=x64 --out=dist --overwrite --prune=true --extra-resource=../frontend --app-version=1.0.0 --win32metadata.FileDescription=Zweig --win32metadata.ProductName=Zweig"
  },
  "devDependencies": {
    "electron": "^32.2.0",
    "@electron/packager": "^18.3.5"
  }
}
